# Root .gitignore for AlbaBiz.ie

# Node / Worker
node_modules/
.wrangler/
cloud/worker/.dev.vars
cloud/worker/.dev.vars.local

# Generated import SQL
import.sql

# Android
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/keystore/
android/.idea/
*.iml

# Signed app binaries — never commit (share out-of-band, not via git)
*.apk
*.aab

# OS / editor
.DS_Store
*.log
.idea/
.vscode/
